server:
  port: 8086

spring:
  application:
    name: mspdi
  datasource:
    url: jdbc:mysql://localhost:3307/bd_mspdi?createDatabaseIfNotExist=true
    username: root
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka